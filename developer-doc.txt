
# test pip with testpypi

# one time setup
python setup.py register -r pypitest

# upload
python setup.py sdist upload -r pypitest

# test in virtualenv
pip install -i https://testpypi.python.org/pypi pipettor
